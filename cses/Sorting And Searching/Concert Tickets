use of Multiset 
use of Upperbound

Multiset is a data structure where we can store multiple elements (same or diff) in sorted order along with we can insert or delete any element in good complexity.
