#include <iostream>
#include <string>
using namespace std;

int main() {
    string str;
    cin >> str;
    int start_pos, length;
    cin >> start_pos >> length;
    string substr = str.substr(start_pos, length); // Extract substring
    cout << "Substring: " << substr << endl;
    return 0;
}
